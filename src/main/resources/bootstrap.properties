spring.application.name=user
# This is the default:
spring.cloud.config.uri=${CONFIG_SERVICE}
spring.cloud.stream.bindings.user-in.group=user-service-group
spring.cloud.stream.bindings.user-out.group=user-service-group
